import React from "react";
import { Link } from "react-router-dom";

const Me = () => {
    return (
        <div>
            <Link to="/create_profile">Создать анкету</Link>
        </div>
    );
};

export default Me;
